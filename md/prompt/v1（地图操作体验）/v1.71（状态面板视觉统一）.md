# v1.71 状态面板视觉统一

## 版本号
v1.71

## 版本分配依据
v1.70 完成单位详情分区。本轮统一补给/士气/经验/威胁与属性条视觉。

## 目标
1. `SupplyPanel`/`MoralePanel`/`ExperiencePanel`/`ThreatSummary`/`StatRows`/`ActionBadge` 对齐指挥台卡片样式与动态字体。
2. 不改 `GameState` 数据；文档 + 云端验收。

## 非目标
- 不改规则计算。
- 不本地 Xcode。
