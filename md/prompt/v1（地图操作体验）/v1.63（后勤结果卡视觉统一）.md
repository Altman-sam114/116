# v1.63 后勤与战术结果卡视觉统一

## 版本号
v1.63

## 版本分配依据
v1.58 已统一攻击反馈卡；v1.61/v1.62 完成标记与命令预览。本轮统一战术命令/据点占领/部署/整补结果卡视觉到战斗结果卡基线。

## 目标
1. `TacticalCommandResultSummaryView`、`ObjectiveCaptureResultSummaryView`、`DeploymentResultSummaryView`、`ReinforcementResultSummaryView` 对齐 `CombatResultSummaryView` 的战报卡层级：标题 + 结论胶囊 + 叙述/主体 + 指标条 + 细节行 + 渐变底与描边。
2. 细节行复用 `BattleResultDetailLine` / `CombatResultDetailRow`。
3. 不改 `GameState`/`GameModels` 结果字段与生成。
4. 同步文档并云端验证。

## 非目标
- 不改反制回放卡（可后续）。
- 不改规则、指标计算。
- 不本地 Xcode。

## 验收
- 四张结果卡视觉与战斗结果卡同族。
- Actions success + Agent C artifact 核对。
