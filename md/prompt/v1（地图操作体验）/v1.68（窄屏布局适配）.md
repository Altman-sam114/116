# v1.68 窄屏布局适配

## 版本号
v1.68

## 版本分配依据
v1.67 完成图例/编队条视觉。本轮按文档建议做窄屏布局适配，改善 iPhone/窄窗口下地图与侧栏可用性。

## 目标
1. 优化 `ContentView` 宽窄断点与 inspector 高度。
2. 窄宽地图下 `MapCampaignHUD`/`MapActionHUD` 改为更省空间布局，减少互相挤压。
3. 保持全部功能入口；不改 `GameState`。
4. 文档 + 云端验收。

## 非目标
- 不引入新第三方布局库。
- 不本地 Xcode。
