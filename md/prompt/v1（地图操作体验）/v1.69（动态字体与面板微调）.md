# v1.69 动态字体与面板微调

## 版本号
v1.69

## 版本分配依据
v1.68 完成窄屏布局。本轮按文档建议推进动态字体友好与侧栏基础面板微调。

## 目标
1. `VictoryPanel`/`ScenarioPanel`/`TileDetail`/`BattleLogView` 对齐战区指挥台视觉。
2. 关键正文使用语义字体 + `minimumScaleFactor`/`lineLimit`，改善动态字体可读性。
3. 胜利重开按钮等主操作保持大点击区。
4. 不改 `GameState`；文档 + 云端验收。

## 非目标
- 不重写全部 HUD 固定字号。
- 不本地 Xcode。
