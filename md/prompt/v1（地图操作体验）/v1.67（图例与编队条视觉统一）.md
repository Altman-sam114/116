# v1.67 图例与编队条视觉统一

## 版本号
v1.67

## 版本分配依据
v1.66 完成侧栏层级与触控。本轮统一地图图例与编队条到战区指挥台视觉基线。

## 目标
1. `MapLegendView`/`LegendItem`/`UnitLegendItem` 使用 `BattlefieldTheme` 卡片质感。
2. `ForceRibbon` 增加分区标签与统一背板；`UnitRibbonButton` 对齐主题描边/选中态。
3. 不改 `GameState`；仅 `ContentView`。
4. 文档 + 云端验收。

## 非目标
- 不改图例条目集合含义；不删图例项。
- 不本地 Xcode。
